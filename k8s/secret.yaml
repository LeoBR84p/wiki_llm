# Template — NÃO faça commit deste arquivo com valores reais.
# Crie o secret via kubectl:
#
#   kubectl create secret generic wiki-llm-secrets \
#     --from-literal=OPENROUTER_APIKEY=sk-or-... \
#     --from-literal=WIKI_UI_STORAGE_SECRET=$(openssl rand -hex 32)
#
# Ou use um gerenciador de segredos (Vault, AWS Secrets Manager, Azure Key Vault).

apiVersion: v1
kind: Secret
metadata:
  name: wiki-llm-secrets
  namespace: wiki-llm
type: Opaque
stringData:
  OPENROUTER_APIKEY: "SUBSTITUA"
  # OPENAI_API_KEY: "SUBSTITUA"
  # AWS_LOGINKEY: "SUBSTITUA"
  # WIKI_BEDROCK_REGION: "sa-east-1"
  WIKI_UI_STORAGE_SECRET: "SUBSTITUA-POR-STRING-ALEATORIA"
